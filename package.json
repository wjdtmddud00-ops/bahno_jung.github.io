{
  "name": "jeong-inhwan-site",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node start-server.js",
    "test": "jest",
    "deploy": "gh-pages -d .",
    "sync": "node sync-and-push.js",
    "serve": "node run-test-server.js",
    "migrate": "node migrate-wall-to-collections.js",
    "build:payload": "node build/create-payload.js",
    "build:sfx-win": "node build/build-windows-sfx.js",
    "build:sfx-mac": "node build/create-mac-sfx.js",
    "build:gui-installer": "npm run build:payload && cd installer-app && npm install && npm run build"
  },
  "devDependencies": {
    "adm-zip": "^0.5.16",
    "archiver": "^7.0.1",
    "gh-pages": "^6.3.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
